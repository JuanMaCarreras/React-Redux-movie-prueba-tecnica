import styled from 'styled-components'

const Button = styled.button`
    background: black;
    border-radius: 1rem;
    cursor: pointer;
    padding-inline: 1rem;
    margin: 9px;
    color: white;

`

export default Button
